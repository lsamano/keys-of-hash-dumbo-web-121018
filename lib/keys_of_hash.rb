class Hash
  def keys_of(*arguments)
    array_of_keys = []
    # take arguments, these are values.
    # checks the given hash for the values.
    Hash.each do |*arguments|
      
    end
    # stores le keys of the matching values.
    # returns array of keys
    array_of_keys
  end
end
